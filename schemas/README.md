# Schemas

This directory contains:

- `workledger.schema.json`: combined JSON Schema definitions for the core V1 objects
- `examples/`: representative payloads for the canonical model

`wl init` also exports a generated schema bundle into the local project directory for inspection.

