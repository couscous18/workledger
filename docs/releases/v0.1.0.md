# Release Notes v0.1.0

`workledger` v0.1.0 is the first public release of the agent work ledger:

- raw traces become human-reviewable `WorkUnit`s
- evidence and uncertainty stay attached
- review queues remain visible instead of being flattened away
- comparative economics stay explicit and editable

PyPI is not published yet. The supported public install path for this release is source install from GitHub.

## Try This In 60 Seconds

```bash
git clone https://github.com/couscous18/workledger.git
cd workledger
uv sync --all-extras
uv run wl demo agent-cost --project-dir .workledger/agent-cost --open-report
uv run wl compare-costs --from-project .workledger/agent-cost
```

## Screenshot

![workledger demo screenshot](../assets/workledger-demo-screenshot.png)

## What Ships In v0.1.0

- canonical models for `ObservationSpan`, `WorkUnit`, `ClassificationTrace`, `PolicyDecision`, `EvidenceRef`, `PolicyPack`, and `ReportArtifact`
- rollup that compresses low-level spans into accountable units of work
- local DuckDB-backed storage plus JSON, CSV, Parquet, Markdown, and HTML outputs
- explainable policy packs for management reporting, software CapEx review, IFRS, US GAAP, and tax R&D
- a local FastAPI server and published OpenAPI artifact
- runnable demos, a proof artifact, and comparative economics reporting

## Public Install Status

- PyPI package: not live yet
- source install from GitHub: supported now
- docs: [https://couscous18.github.io/workledger/](https://couscous18.github.io/workledger/)
- proof artifact: [../assets/builder-demo-report.html](../assets/builder-demo-report.html)

## Suggested GitHub Release Body

````md
## workledger v0.1.0

`workledger` is an agent work ledger for AI systems. It turns raw traces into reviewable `WorkUnit`s with evidence, policy context, and transparent economics.

### Try this in 60 seconds

```bash
git clone https://github.com/couscous18/workledger.git
cd workledger
uv sync --all-extras
uv run wl demo agent-cost --project-dir .workledger/agent-cost --open-report
uv run wl compare-costs --from-project .workledger/agent-cost
```

### Install status

PyPI is not published yet. Please install from source from this repository for now.

### What ships

- roll up traces into human-reviewable `WorkUnit`s
- keep evidence and review state attached
- run explainable policy-backed classification
- compare observed usage against editable economics scenarios
- generate report bundles, including HTML output you can share internally
````
