# Release Notes v0.1.0

`workledger` v0.1.0 is the first public release of the trace-to-work layer.

## Summary

This release makes the project publicly usable from source with the core local-first trace-to-work model, ingestion pipeline, rollup engine, report outputs, and docs.

The important story for this release is:

- public traces become reviewable `WorkUnit`s
- Hugging Face datasets are first-class demo inputs
- evidence, lineage, and uncertainty stay attached
- review-needed states remain visible instead of being flattened away
- economics remain a downstream interpretation of attributed work

## Quick Install And Try

PyPI distribution is not live for this release. Install from source from this repository:

```bash
git clone https://github.com/couscous18/workledger.git
cd workledger
uv sync --all-extras
uv run wl demo hf-gaia --project-dir .workledger/hf-gaia --open-report
```

Optional second demo:

```bash
uv run wl demo hf-smoltrace --project-dir .workledger/hf-smoltrace --open-report
```

Release-linked demo artifacts:

- [GAIA demo report](../assets/hf-gaia-demo-report.html)
- [smoltrace demo report](../assets/hf-smoltrace-demo-report.html)

## What Ships In v0.1.0

- Core models for `ObservationSpan`, `WorkUnit`, `ClassificationTrace`, `PolicyDecision`, `EvidenceRef`, `PolicyPack`, and `ReportArtifact`
- Hugging Face ingestion adapters and public demo paths for `smolagents/gaia-traces` and `kshitijthakkar/smoltrace-traces-20260130_053009`
- Ingestion for JSONL, OpenInference-like payloads, OTEL JSON, CloudEvents JSON, and SDK-shaped events
- Rollup that compresses low-level spans into reviewable work units
- Declarative policy packs with explainable rule matches and review-required states
- DuckDB-backed local store with JSON, CSV, and Parquet export plus terminal, Markdown, JSON, CSV, Parquet, and HTML reports
- `wl` CLI, local FastAPI server, runnable demos, JSON Schema export, and published OpenAPI artifacts
- Downstream comparative economics and software capex examples built on the same attributed-work layer

## Alpha Status

`workledger` v0.1.0 is an alpha release. It is intended to make the project publicly usable and reviewable, not to promise stable interfaces yet.

## Known Limitations

- Alpha quality: rough edges are expected
- Breaking changes may happen before a stable `1.0.0`
- Local or source install is required until package distribution is live
- `smolagents/codeagent-traces` is a planned next adapter target, not implemented in v0.1.0
